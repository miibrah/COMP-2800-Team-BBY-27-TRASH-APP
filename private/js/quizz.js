const mongoose = require('mongoose')
const quizzSchema = require ('../DB/Question.js');
describe ('Saving records', function(){

    it ('Saves a record to the database', function(){

        var question = new quizzSchema ({
            
        })
    })

});

